def CONTAINER_NAME="jenkins-pipeline"
def CONTAINER_TAG="latest"

pipeline {
    agent any
    stages {
        stage('Image Build'){
             steps {
                sh "docker --version"
                echo "Image build complete"
             }
        }
        
    }
}
